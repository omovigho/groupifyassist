"""
Legacy auth router stub

This file used to contain experimental authentication endpoints.
The active authentication and user-related endpoints live in `routes/user.py`.
We keep this stub to avoid import errors from stale references.
"""

from fastapi import APIRouter

# Intentionally empty router. Do not include in main app.
router = APIRouter()
